# Vault Interfaces

This directory contains interface crates for external vault contracts that YBC integrates with.

- **vault_interface** - Interface for SEP56 vaults (e.g. Blend)

This is used by the Yield Manager to query vault exchange rates and interact with vault contracts.